# python-input_output

This project covers reading and writing files in Python, using the
`with` statement for automatic resource cleanup, and JSON
serialization/deserialization to convert Python data structures to
and from JSON strings and files.
